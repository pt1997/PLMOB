# configuration for the mobile-sway-mode

# include plmob defaults 
include ~/.config/sway/config.d/*

# basic settings
default_border none

# include defaults
include /etc/sway/config.d/*

#switch mode
bindsym $mod+m exec "ln -sf desktop-mode ~/.config/sway/config; swaymsg reload"
